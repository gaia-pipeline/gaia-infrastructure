apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: cronohub
  name: do-storage-gaia
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: do-block-storage
