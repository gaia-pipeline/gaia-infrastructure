---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: cronohub
  name: gaia-app
  labels:
    app: gaia-app
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/port: '9090'
    fluxcd.io/automated: 'true'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gaia-app
  template:
    metadata:
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '9090'
      labels:
        app: gaia-app
        app.kubernetes.io/name: gaia-app
        app.kubernetes.io/instance: gaia-app
    spec:
      containers:
      - name: gaia
        image: gaiapipeline/testing:4a54dc395bd631ddf7a1a6b4dfbe73c21bc8447a20595b4f4e3b2907d3157a6a
        ports:
        - containerPort: 8080
          name: gaia-http
        volumeMounts:
        - mountPath: /data
          name: gaia-data
          readOnly: false
        args:
        - -hostname="https://gaia.cronohub.org"
      volumes:
      - name: gaia-data
        persistentVolumeClaim:
          claimName: do-storage-gaia
